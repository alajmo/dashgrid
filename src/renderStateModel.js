export {renderStateModel};

function renderStateModel () {
    // Start row / column denotes the pixel at which each cell starts at.
    let renderStateModel = {
        startColumn: [],
        startRow: [],
        columnWidth: undefined,
        rowHeight: undefined  
    };

    return renderStateModel;
}
